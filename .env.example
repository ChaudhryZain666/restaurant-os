# Used by docker-compose.yml to inject secrets into the api container.
# For non-Docker local dev, apps/api/.env is used instead (see apps/api/.env.example).
JWT_ACCESS_SECRET=replace-with-a-random-64-char-hex-string
JWT_REFRESH_SECRET=replace-with-a-different-random-64-char-hex-string
